class Song {
    constructor(id, name, previewUrl, duration) {
        this.id = id;
        this.name = name;
        this.previewUrl = previewUrl;
        this.duration = duration;
    }
}

export default Song;