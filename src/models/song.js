class Song {
    constructor(id, name, previewUrl, duration, artist) {
        this.id = id;
        this.name = name;
        this.previewUrl = previewUrl;
        this.duration = duration;
        this.artist = artist;
    }
}

export default Song;